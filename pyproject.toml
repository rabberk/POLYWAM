[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "polywam-training"
description = "Anonymous training source snapshot"
version = "0.0.1"
readme = "README.md"
requires-python = ">=3.10"
keywords = ["vision-language-action", "robot learning", "V-JEPA", "LIBERO"]
license = {file = "LICENSE"}
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Education",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3 :: Only",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "dlimp @ git+https://github.com/moojink/dlimp_openvla.git@040105d256bd28866cc6620621a3d5f7b6b91b46",
    "accelerate>=0.26",
    "rich>=13",
    "packaging>=23",
    "absl-py>=2.0",
    "av>=12,<15",
    "diffusers>=0.30",
    "draccus>=0.8",
    "jsonlines>=4.0",
    "numpy>=1.26,<2",
    "pyarrow>=15,<20",
    "peft>=0.13",
    "pillow>=10",
    "psutil>=5.9",
    "pyyaml>=6.0",
    "swanlab>=0.6",
    "tensorflow>=2.15,<2.16",
    "tensorflow-datasets>=4.9",
    "torch>=2.2",
    "torchvision>=0.17",
    "tqdm>=4.66",
    "transformers>=4.45,<5",
]

[project.optional-dependencies]
dev = [
    "black>=24.2.0",
    "gpustat",
    "ipython",
    "pre-commit",
    "ruff>=0.2.2",
]
[tool.setuptools.packages.find]
where = ["."]
include = ["prismatic*"]
namespaces = false

[tool.setuptools.package-data]
"prismatic" = ["py.typed"]

[tool.black]
line-length = 121
target-version = ["py310"]
preview = true

[tool.ruff]
line-length = 121
target-version = "py310"

[tool.ruff.lint]
select = ["A", "B", "E", "F", "I", "RUF", "W"]
ignore = ["F722"]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["E402", "F401"]
